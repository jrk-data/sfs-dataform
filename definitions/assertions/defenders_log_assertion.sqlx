config {
  type: "assertion",
  dependencies:"defenders_log",
  }


SELECT  True as updated
FROM ${schemas.ods_project}.${schemas.sfs}.${schemas.ods_defenders_log}
WHERE date(ingestedAt) = CURRENT_DATE()